language: objective-c
osx_image: xcode8

build: zip -r InMoment.framework.zip InMoment.framework

deploy:
  - skip_cleanup: true
  - provider: releases
    api_key: $GH_TOKEN
    prerelease: true
    file: InMoment.framework.zip
    on:
      tags: true
  - provider: script
    script: pod trunk push
    on:
      tags: true
